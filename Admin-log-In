#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
using namespace std;

struct users
{
	
};

vector<users>user; 

int main()
{
   int attempts = 0;

   do
   {
      cout << "\n:: Admin Log-in ::\n";

      cout << "\nUsername: ";
      cin >> username;
      cout << "\nPassword: ";
      cin >> password;
      if (username != "" || password != "Pass")
      {
         cout << "Username or Password Incorrect" << endl;
         attempts++;
      }
      else
      {
         cout << "Welcome " << endl;
      }
   } while (username != userCheck && password != passCheck || attempts != 3); 

   if (attempts == 3)
   {
      cout << "\nYou have failed to imput correct"
      main();
   }
}
