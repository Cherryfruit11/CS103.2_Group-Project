#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
using namespace std;

void adminLogin();
void teacherLogin();
void parentLogin();
void registr();
void changePassword(); 

struct users
{
	string username, password;
};

vector<users>user; 

int main()
{
   string username = "";
   string password = "";
bool loginSucess = false; 

do
{
cout << "Username: ";
cin >> username;
cout << "\nPassword: ";
cin >> password;

if (username != "" || password != "Pass")
{
cout << "Username or Password Incorrect" << endl;
}
else
{
cout << "Welcome " << endl;
}

} while (username != "" && password != "Pass"); 

do
{
char opt;

cout << "Please select what you want to do";
cin >> opt;

switch (opt)
{
case 's':
{
cout << "information"; //add however many we need
break; 
}
}
}
